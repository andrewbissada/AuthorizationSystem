// Define a class for Card to store its ID and Balance
public class Card
{
    // Store the 15-digit ID of the card
    public string Id { get; set; }
    // Store the current balance on the card
    public decimal Balance { get; set; }
}
